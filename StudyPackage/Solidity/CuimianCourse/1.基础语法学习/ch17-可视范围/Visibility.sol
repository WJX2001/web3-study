// SPDX-License-Identifier: MIT
pragma solidity 0.8.7;

// TODO: 可视范围
/**
  private： 私有（只在合约内部可见）  only inside contract 
  internal： 内部（合约的内部和被继承的子合约都可以见） only inside contract and child contracts
  external： 外部（仅在外部调用可看到，内部看不到）inside and outside contract
  public： 公开（内部被继承、外部都可见）  only from outside contract
 */

